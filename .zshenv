#==========================================================#
# 環境変数
#==========================================================#
if [[ -f "$HOME/.config/secrets" ]];then
  . "$HOME/.config/secrets"
fi
